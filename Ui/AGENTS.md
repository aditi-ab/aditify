# Aditify UI

These instructions apply to the public `@aditify/ui` package.

## Validation

- Run `yarn lint:fix`, `yarn type-check`, `yarn test`, and `yarn build` from this workspace after UI changes.
